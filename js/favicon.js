(window.requestIdleCallback||function(cb){setTimeout(cb,200);})(function(){const S=64;const c=document.createElement('canvas');c.width=S;c.height=S;const ctx=c.getContext('2d');const cx=S/2,cy=S/2;const turns=2.2;const maxR=S*0.48;const minR=S*0.03;const steps=1200;const pts=[];for(let i=0;i<=steps;i++){const t=i/steps;const angle=t*turns*Math.PI*2-Math.PI*0.5;const r=minR+(maxR-minR)*t;pts.push({x:cx+Math.cos(angle)*r,y:cy+Math.sin(angle)*r});}ctx.lineCap='round';ctx.lineJoin='round';ctx.shadowColor='rgba(0,200,40,0.20)';ctx.shadowBlur=2;for(let i=0;i<steps;i++){const t=i/steps;const endTaper=t>0.90?Math.sin((1-t)/0.10*Math.PI*0.5):1.0;const lw=(5.0+endTaper*8.0)*(S/320);const alpha=0.65+endTaper*0.27;const green=Math.floor(190+endTaper*20);ctx.strokeStyle=`rgba(0,${green},42,${alpha})`;ctx.lineWidth=lw;ctx.beginPath();ctx.moveTo(pts[i].x,pts[i].y);ctx.lineTo(pts[i+1].x,pts[i+1].y);ctx.stroke();}ctx.shadowBlur=0;const link=document.createElement('link');link.rel='icon';link.href=c.toDataURL();document.head.appendChild(link);document.querySelectorAll('link[rel="icon"]').forEach((l,i,a)=>{if(i<a.length-1)l.remove();});});